const unit = 4;

const calcRem = (size) => `${size / 16}rem`;

const color = {
  text: '#1d1d1f',
  hover: '#06c',
};

const sizing = {};

const theme = {
  color,
  sizing,
};

export default theme;
